apiVersion: v1
kind: Service
metadata:
  name: multi-region-app
  namespace: multi-region
  labels:
    app: multi-region-app
spec:
  type: LoadBalancer
  selector:
    app: multi-region-app
  ports:
    - name: http
      port: 80
      targetPort: 8000
